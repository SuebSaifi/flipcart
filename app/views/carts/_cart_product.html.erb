<%@cart.line_items.each do |cart|%>
<tr>
    <td>
        <%=cart.product.title%>
    </td>
    <td>
        <%=cart.product.price%>
    </td>
    <td>
   <%= cart.quantity.to_i %>
    </td>
    <td>
       <%= number_to_currency(cart.total_price) %>
    </td>
    <td>
        <%= button_to "Add one", line_item_add_path(:id => cart), method: :post %>
        </td>
        <td>  
        <%= button_to "Reduce one", line_item_reduce_path(:id => cart), method: :post %>
        </td>
        <td>    
        <%= button_to "Remove item", line_item_path(cart), method: :delete %>
    </td>
     <td>
       <%# <%=button_to 'Buy Now',checkout_create_path,method: :post,params:{id: cart.product.id},remote: true%>

        <%# <%=link_to "Buy" ,"/users/#{current_user.id}/order",method: :get,params:[product_id:cart.product.id],class:"btn btn-success"%>
    </td>
</tr>
<%end%>

<%if @cart.line_items.present?%>


       <%# <%= link_to "Proceed to checkout", new_order_path ,class:"btn btn-info"%><br>
    
       <%end%>
       <%=button_to 'Buy Now',checkout_create_path,method: :post,remote: true,class:"sm"%>
       
<%# <%=@current_cart.line_items.collect{|i|i.to_builder.attributes!} %>